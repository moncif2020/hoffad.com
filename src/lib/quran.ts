export const QURAN_SURAHS = [
  { "number": 1, "name": "سُورَةُ ٱلْفَاتِحَةِ", "englishName": "Al-Fatihah", "numberOfAyahs": 7 },
  { "number": 2, "name": "سُورَةُ البَقَرَةِ", "englishName": "Al-Baqarah", "numberOfAyahs": 286 },
  { "number": 3, "name": "سُورَةُ آلِ عِمۡرَانَ", "englishName": "Ali 'Imran", "numberOfAyahs": 200 },
  { "number": 4, "name": "سُورَةُ النِّسَاءِ", "englishName": "An-Nisa", "numberOfAyahs": 176 },
  { "number": 5, "name": "سُورَةُ المَائـِدَةِ", "englishName": "Al-Ma'idah", "numberOfAyahs": 120 },
  { "number": 6, "name": "سُورَةُ الأَنۡعَامِ", "englishName": "Al-An'am", "numberOfAyahs": 165 },
  { "number": 7, "name": "سُورَةُ الأَعۡرَافِ", "englishName": "Al-A'raf", "numberOfAyahs": 206 },
  { "number": 8, "name": "سُورَةُ الأَنفَالِ", "englishName": "Al-Anfal", "numberOfAyahs": 75 },
  { "number": 9, "name": "سُورَةُ التَّوۡبَةِ", "englishName": "At-Tawbah", "numberOfAyahs": 129 },
  { "number": 10, "name": "سُورَةُ يُونُسَ", "englishName": "Yunus", "numberOfAyahs": 109 },
  { "number": 11, "name": "سُورَةُ هُودٍ", "englishName": "Hud", "numberOfAyahs": 123 },
  { "number": 12, "name": "سُورَةُ يُوسُفَ", "englishName": "Yusuf", "numberOfAyahs": 111 },
  { "number": 13, "name": "سُورَةُ الرَّعۡدِ", "englishName": "Ar-Ra'd", "numberOfAyahs": 43 },
  { "number": 14, "name": "سُورَةُ إِبۡرَاهِيمَ", "englishName": "Ibrahim", "numberOfAyahs": 52 },
  { "number": 15, "name": "سُورَةُ الحِجۡرِ", "englishName": "Al-Hijr", "numberOfAyahs": 99 },
  { "number": 16, "name": "سُورَةُ النَّحۡلِ", "englishName": "An-Nahl", "numberOfAyahs": 128 },
  { "number": 17, "name": "سُورَةُ الإِسۡرَاءِ", "englishName": "Al-Isra", "numberOfAyahs": 111 },
  { "number": 18, "name": "سُورَةُ الكَهۡفِ", "englishName": "Al-Kahf", "numberOfAyahs": 110 },
  { "number": 19, "name": "سُورَةُ مَرۡيَمَ", "englishName": "Maryam", "numberOfAyahs": 98 },
  { "number": 20, "name": "سُورَةُ طه", "englishName": "Taha", "numberOfAyahs": 135 },
  { "number": 21, "name": "سُورَةُ الأَنبِيَاءِ", "englishName": "Al-Anbiya", "numberOfAyahs": 112 },
  { "number": 22, "name": "سُورَةُ الحَجِّ", "englishName": "Al-Hajj", "numberOfAyahs": 78 },
  { "number": 23, "name": "سُورَةُ المُؤۡمِنُونَ", "englishName": "Al-Mu'minun", "numberOfAyahs": 118 },
  { "number": 24, "name": "سُورَةُ النُّورِ", "englishName": "An-Nur", "numberOfAyahs": 64 },
  { "number": 25, "name": "سُورَةُ الفُرۡقَانِ", "englishName": "Al-Furqan", "numberOfAyahs": 77 },
  { "number": 26, "name": "سُورَةُ الشُّعَرَاءِ", "englishName": "Ash-Shu'ara", "numberOfAyahs": 227 },
  { "number": 27, "name": "سُورَةُ النَّمۡلِ", "englishName": "An-Naml", "numberOfAyahs": 93 },
  { "number": 28, "name": "سُورَةُ القَصَصِ", "englishName": "Al-Qasas", "numberOfAyahs": 88 },
  { "number": 29, "name": "سُورَةُ العَنكَبُوتِ", "englishName": "Al-'Ankabut", "numberOfAyahs": 69 },
  { "number": 30, "name": "سُورَةُ الرُّومِ", "englishName": "Ar-Rum", "numberOfAyahs": 60 },
  { "number": 31, "name": "سُورَةُ لُقۡمَانَ", "englishName": "Luqman", "numberOfAyahs": 34 },
  { "number": 32, "name": "سُورَةُ السَّجۡدَةِ", "englishName": "As-Sajdah", "numberOfAyahs": 30 },
  { "number": 33, "name": "سُورَةُ الأَحۡزَابِ", "englishName": "Al-Ahzab", "numberOfAyahs": 73 },
  { "number": 34, "name": "سُورَةُ سَبَإٍ", "englishName": "Saba", "numberOfAyahs": 54 },
  { "number": 35, "name": "سُورَةُ فَاطِرٍ", "englishName": "Fatir", "numberOfAyahs": 45 },
  { "number": 36, "name": "سُورَةُ يسٓ", "englishName": "Ya-Sin", "numberOfAyahs": 83 },
  { "number": 37, "name": "سُورَةُ الصَّافَّاتِ", "englishName": "As-Saffat", "numberOfAyahs": 182 },
  { "number": 38, "name": "سُورَةُ صٓ", "englishName": "Sad", "numberOfAyahs": 88 },
  { "number": 39, "name": "سُورَةُ الزُّمَرِ", "englishName": "Az-Zumar", "numberOfAyahs": 75 },
  { "number": 40, "name": "سُورَةُ غَافِرٍ", "englishName": "Ghafir", "numberOfAyahs": 85 },
  { "number": 41, "name": "سُورَةُ فُصِّلَتۡ", "englishName": "Fussilat", "numberOfAyahs": 54 },
  { "number": 42, "name": "سُورَةُ الشُّورَىٰ", "englishName": "Ash-Shura", "numberOfAyahs": 53 },
  { "number": 43, "name": "سُورَةُ الزُّخۡرُفِ", "englishName": "Az-Zukhruf", "numberOfAyahs": 89 },
  { "number": 44, "name": "سُورَةُ الدُّخَانِ", "englishName": "Ad-Dukhan", "numberOfAyahs": 59 },
  { "number": 45, "name": "سُورَةُ الجَاثِيَةِ", "englishName": "Al-Jathiyah", "numberOfAyahs": 37 },
  { "number": 46, "name": "سُورَةُ الأَحۡقَافِ", "englishName": "Al-Ahqaf", "numberOfAyahs": 35 },
  { "number": 47, "name": "سُورَةُ مُحَمَّدٍ", "englishName": "Muhammad", "numberOfAyahs": 38 },
  { "number": 48, "name": "سُورَةُ الفَتۡحِ", "englishName": "Al-Fath", "numberOfAyahs": 29 },
  { "number": 49, "name": "سُورَةُ الحُجُرَاتِ", "englishName": "Al-Hujurat", "numberOfAyahs": 18 },
  { "number": 50, "name": "سُورَةُ قٓ", "englishName": "Qaf", "numberOfAyahs": 45 },
  { "number": 51, "name": "سُورَةُ الذَّارِيَاتِ", "englishName": "Adh-Dhariyat", "numberOfAyahs": 60 },
  { "number": 52, "name": "سُورَةُ الطُّورِ", "englishName": "At-Tur", "numberOfAyahs": 49 },
  { "number": 53, "name": "سُورَةُ النَّجۡمِ", "englishName": "An-Najm", "numberOfAyahs": 62 },
  { "number": 54, "name": "سُورَةُ القَمَرِ", "englishName": "Al-Qamar", "numberOfAyahs": 55 },
  { "number": 55, "name": "سُورَةُ الرَّحۡمَٰن", "englishName": "Ar-Rahman", "numberOfAyahs": 78 },
  { "number": 56, "name": "سُورَةُ الوَاقِعَةِ", "englishName": "Al-Waqi'ah", "numberOfAyahs": 96 },
  { "number": 57, "name": "سُورَةُ الحَدِيدِ", "englishName": "Al-Hadid", "numberOfAyahs": 29 },
  { "number": 58, "name": "سُورَةُ المُجَادلَةِ", "englishName": "Al-Mujadilah", "numberOfAyahs": 22 },
  { "number": 59, "name": "سُورَةُ الحَشۡرِ", "englishName": "Al-Hashr", "numberOfAyahs": 24 },
  { "number": 60, "name": "سُورَةُ المُمۡتَحنَةِ", "englishName": "Al-Mumtahanah", "numberOfAyahs": 13 },
  { "number": 61, "name": "سُورَةُ الصَّفِّ", "englishName": "As-Saff", "numberOfAyahs": 14 },
  { "number": 62, "name": "سُورَةُ الجُمُعَةِ", "englishName": "Al-Jumu'ah", "numberOfAyahs": 11 },
  { "number": 63, "name": "سُورَةُ المُنَافِقُونَ", "englishName": "Al-Munafiqun", "numberOfAyahs": 11 },
  { "number": 64, "name": "سُورَةُ التَّغَابُنِ", "englishName": "At-Taghabun", "numberOfAyahs": 18 },
  { "number": 65, "name": "سُورَةُ الطَّلَاقِ", "englishName": "At-Talaq", "numberOfAyahs": 12 },
  { "number": 66, "name": "سُورَةُ التَّحۡرِيمِ", "englishName": "At-Tahrim", "numberOfAyahs": 12 },
  { "number": 67, "name": "سُورَةُ المُلۡكِ", "englishName": "Al-Mulk", "numberOfAyahs": 30 },
  { "number": 68, "name": "سُورَةُ القَلَمِ", "englishName": "Al-Qalam", "numberOfAyahs": 52 },
  { "number": 69, "name": "سُورَةُ الحَاقَّةِ", "englishName": "Al-Haqqah", "numberOfAyahs": 52 },
  { "number": 70, "name": "سُورَةُ المَعَارِجِ", "englishName": "Al-Ma'arij", "numberOfAyahs": 44 },
  { "number": 71, "name": "سُورَةُ نُوحٍ", "englishName": "Nuh", "numberOfAyahs": 28 },
  { "number": 72, "name": "سُورَةُ الجِنِّ", "englishName": "Al-Jinn", "numberOfAyahs": 28 },
  { "number": 73, "name": "سُورَةُ المُزَّمِّلِ", "englishName": "Al-Muzzammil", "numberOfAyahs": 20 },
  { "number": 74, "name": "سُورَةُ المُدَّثِّرِ", "englishName": "Al-Muddaththir", "numberOfAyahs": 56 },
  { "number": 75, "name": "سُورَةُ القِيَامَةِ", "englishName": "Al-Qiyamah", "numberOfAyahs": 40 },
  { "number": 76, "name": "سُورَةُ الإِنسَانِ", "englishName": "Al-Insan", "numberOfAyahs": 31 },
  { "number": 77, "name": "سُورَةُ المُرۡسَلَاتِ", "englishName": "Al-Mursalat", "numberOfAyahs": 50 },
  { "number": 78, "name": "سُورَةُ النَّبَإِ", "englishName": "An-Naba", "numberOfAyahs": 40 },
  { "number": 79, "name": "سُورَةُ النَّازِعَاتِ", "englishName": "An-Nazi'at", "numberOfAyahs": 46 },
  { "number": 80, "name": "سُورَةُ عَبَسَ", "englishName": "'Abasa", "numberOfAyahs": 42 },
  { "number": 81, "name": "سُورَةُ التَّكۡوِيرِ", "englishName": "At-Takwir", "numberOfAyahs": 29 },
  { "number": 82, "name": "سُورَةُ الانفِطَارِ", "englishName": "Al-Infitar", "numberOfAyahs": 19 },
  { "number": 83, "name": "سُورَةُ المُطَفِّفِينَ", "englishName": "Al-Mutaffifin", "numberOfAyahs": 36 },
  { "number": 84, "name": "سُورَةُ الانشِقَاقِ", "englishName": "Al-Inshiqaq", "numberOfAyahs": 25 },
  { "number": 85, "name": "سُورَةُ البُرُوجِ", "englishName": "Al-Buruj", "numberOfAyahs": 22 },
  { "number": 86, "name": "سُورَةُ الطَّارِقِ", "englishName": "At-Tariq", "numberOfAyahs": 17 },
  { "number": 87, "name": "سُورَةُ الأَعۡلَىٰ", "englishName": "Al-A'la", "numberOfAyahs": 19 },
  { "number": 88, "name": "سُورَةُ الغَاشِيَةِ", "englishName": "Al-Ghashiyah", "numberOfAyahs": 26 },
  { "number": 89, "name": "سُورَةُ الفَجۡرِ", "englishName": "Al-Fajr", "numberOfAyahs": 30 },
  { "number": 90, "name": "سُورَةُ البَلَدِ", "englishName": "Al-Balad", "numberOfAyahs": 20 },
  { "number": 91, "name": "سُورَةُ الشَّمۡسِ", "englishName": "Ash-Shams", "numberOfAyahs": 15 },
  { "number": 92, "name": "سُورَةُ اللَّيۡلِ", "englishName": "Al-Layl", "numberOfAyahs": 21 },
  { "number": 93, "name": "سُورَةُ الضُّحَىٰ", "englishName": "Ad-Duha", "numberOfAyahs": 11 },
  { "number": 94, "name": "سُورَةُ الشَّرۡحِ", "englishName": "Ash-Sharh", "numberOfAyahs": 8 },
  { "number": 95, "name": "سُورَةُ التِّينِ", "englishName": "At-Tin", "numberOfAyahs": 8 },
  { "number": 96, "name": "سُورَةُ العَلَقِ", "englishName": "Al-'Alaq", "numberOfAyahs": 19 },
  { "number": 97, "name": "سُورَةُ القَدۡرِ", "englishName": "Al-Qadr", "numberOfAyahs": 5 },
  { "number": 98, "name": "سُورَةُ البَيِّنَةِ", "englishName": "Al-Bayyinah", "numberOfAyahs": 8 },
  { "number": 99, "name": "سُورَةُ الزَّلۡزَلَةِ", "englishName": "Az-Zalzalah", "numberOfAyahs": 8 },
  { "number": 100, "name": "سُورَةُ العَادِيَاتِ", "englishName": "Al-'Adiyat", "numberOfAyahs": 11 },
  { "number": 101, "name": "سُورَةُ القَارِعَةِ", "englishName": "Al-Qari'ah", "numberOfAyahs": 11 },
  { "number": 102, "name": "سُورَةُ التَّكَاثُرِ", "englishName": "At-Takathur", "numberOfAyahs": 8 },
  { "number": 103, "name": "سُورَةُ العَصۡرِ", "englishName": "Al-'Asr", "numberOfAyahs": 3 },
  { "number": 104, "name": "سُورَةُ الهُمَزَةِ", "englishName": "Al-Humazah", "numberOfAyahs": 9 },
  { "number": 105, "name": "سُورَةُ الفِيلِ", "englishName": "Al-Fil", "numberOfAyahs": 5 },
  { "number": 106, "name": "سُورَةُ قُرَيۡشٍ", "englishName": "Quraysh", "numberOfAyahs": 4 },
  { "number": 107, "name": "سُورَةُ المَاعُونِ", "englishName": "Al-Ma'un", "numberOfAyahs": 7 },
  { "number": 108, "name": "سُورَةُ الكَوۡثَرِ", "englishName": "Al-Kawthar", "numberOfAyahs": 3 },
  { "number": 109, "name": "سُورَةُ الكَافِرُونَ", "englishName": "Al-Kafirun", "numberOfAyahs": 6 },
  { "number": 110, "name": "سُورَةُ النَّصۡرِ", "englishName": "An-Nasr", "numberOfAyahs": 3 },
  { "number": 111, "name": "سُورَةُ المَسَدِ", "englishName": "Al-Masad", "numberOfAyahs": 5 },
  { "number": 112, "name": "سُورَةُ الإِخۡلَاصِ", "englishName": "Al-Ikhlas", "numberOfAyahs": 4 },
  { "number": 113, "name": "سُورَةُ الفَلَقِ", "englishName": "Al-Falaq", "numberOfAyahs": 5 },
  { "number": 114, "name": "سُورَةُ النَّاسِ", "englishName": "An-Nas", "numberOfAyahs": 6 }
];

export const getAudioUrl = (reciterCode: string, surahNum: number | null, ayahInSurah: number | null, mirrorIndex: number = 0) => {
  if (surahNum === null || ayahInSurah === null) return "";
  const surahStr = surahNum.toString().padStart(3, '0');
  const ayahStr = ayahInSurah.toString().padStart(3, '0');
  
  // Handle full URL reciters (from mp3quran.net etc)
  if (reciterCode.startsWith('http')) {
    // These usually follow a pattern like baseUrl/100001.mp3
    return `${reciterCode}${surahStr}${ayahStr}.mp3`;
  }

  const mirrors = [
    `https://everyayah.com/data/${reciterCode}/${surahStr}${ayahStr}.mp3`,
    `https://www.everyayah.com/data/${reciterCode}/${surahStr}${ayahStr}.mp3`,
    `https://mirrors.quranicaudio.com/everyayah/data/${reciterCode}/${surahStr}${ayahStr}.mp3`
  ];

  return mirrors[mirrorIndex % mirrors.length];
};

async function fetchWithRetry(url: string, options: RequestInit = {}, retries = 2): Promise<Response> {
  try {
    const response = await fetch(url, options);
    if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);
    return response;
  } catch (error) {
    if (retries > 0) {
      // Exponential backoff
      await new Promise(resolve => setTimeout(resolve, 1000 * (3 - retries)));
      return fetchWithRetry(url, options, retries - 1);
    }
    throw error;
  }
}

export async function safeJson(response: Response) {
  const text = await response.text();
  if (!text) throw new Error("Empty response body");
  try {
    return JSON.parse(text);
  } catch (e) {
    console.error("JSON parse error. Body snippet:", text.slice(0, 200));
    throw e;
  }
}

export const fetchAyahs = async (surahNum: number, from: number, to: number) => {
  if (!surahNum || surahNum < 1 || surahNum > 114) {
    console.error(`fetchAyahs called with invalid surahNum: ${surahNum}`);
    throw new Error(`Invalid surah number: ${surahNum}`);
  }

  try {
    // Primary source: alquran.cloud
    try {
      const response = await fetchWithRetry(`https://api.alquran.cloud/v1/surah/${surahNum}/editions/quran-uthmani`);
      const data = await safeJson(response);
      
      if (!data || !data.data || !Array.isArray(data.data) || data.data.length === 0) {
        throw new Error("Invalid data structure from alquran.cloud");
      }

      const surah = data.data[0];
      if (!surah) throw new Error("Surah object is null from alquran.cloud");
      
      return {
        surahName: surah.name || `Sura ${surahNum}`,
        ayahs: (surah.ayahs || []).slice(from - 1, to).map((a: any) => ({
          number: a.number,
          numberInSurah: a.numberInSurah,
          text: a.text
        }))
      };
    } catch (e) {
      console.warn('Primary API failed, trying fallback source...', e);
      // Fallback source: api.quran.com
      const response = await fetchWithRetry(`https://api.quran.com/api/v4/quran/verses/uthmani?chapter_number=${surahNum}`);
      const data = await safeJson(response);
      const surahData = QURAN_SURAHS[surahNum - 1];
      
      if (!data || !data.verses) throw new Error("Invalid data structure from api.quran.com");

      return {
        surahName: surahData?.name || `سورة ${surahNum}`,
        ayahs: data.verses.slice(from - 1, to).map((v: any) => ({
          number: v.id,
          numberInSurah: parseInt(v.verse_key?.split(':')[1] || "0"),
          text: v.text_uthmani
        }))
      };
    }
  } catch (error) {
    console.error('Final error fetching ayahs:', error);
    throw error;
  }
};

export const downloadSurahAudio = async (
  surahNum: number, 
  from: number, 
  to: number, 
  reciter: string, 
  onProgress?: (progress: number) => void,
  signal?: AbortSignal
) => {
  const total = to - from + 1;
  const cache = await caches.open('quran-audio');
  
  for (let i = from; i <= to; i++) {
    if (signal?.aborted) throw new Error('Aborted');
    
    const url = getAudioUrl(reciter, surahNum, i);
    const cached = await cache.match(url);
    if (!cached) {
      try {
        const response = await fetch(url, { signal });
        if (response.ok) {
          await cache.put(url, response);
        }
      } catch (e) {
        if ((e as Error).name === 'AbortError') throw e;
        console.error(`Failed to download ayah ${i} of surah ${surahNum}`, e);
      }
    }
    if (onProgress) {
      onProgress(Math.round(((i - from + 1) / total) * 100));
    }
  }
};

export const downloadFullQuranAudio = async (
  reciter: string,
  onProgress?: (progress: number, surahName: string) => void,
  signal?: AbortSignal
) => {
  let totalAyahsDownloaded = 0;
  const totalAyahs = 6236;

  for (const surah of QURAN_SURAHS) {
    if (signal?.aborted) throw new Error('Aborted');
    
    await downloadSurahAudio(
      surah.number,
      1,
      surah.numberOfAyahs,
      reciter,
      (_) => {
        // We track overall progress based on ayahs
      },
      signal
    );
    
    totalAyahsDownloaded += surah.numberOfAyahs;
    if (onProgress) {
      onProgress(Math.round((totalAyahsDownloaded / totalAyahs) * 100), surah.name);
    }
  }
};

export const isRangeDownloaded = async (surahNum: number | null, from: number | null, to: number | null, reciter: string) => {
  if (surahNum === null || from === null || to === null) return false;
  try {
    const cache = await caches.open('quran-audio');
    for (let i = from; i <= to; i++) {
      const url = getAudioUrl(reciter, surahNum, i);
      const cached = await cache.match(url);
      if (!cached) return false;
    }
    return true;
  } catch (e) {
    console.error("Cache check failed:", e);
    return false;
  }
};

// Cache for local search to ensure accuracy and speed
let fullQuranSearchIndex: any[] | null = null;
let isFetchingIndex = false;

// Helper to normalize English/Latin text (remove hyphens, spaces, and single quotes)
export const normalizeLatin = (text: string): string => {
  if (!text) return "";
  return text.toLowerCase()
    .replace(/[-\s']/g, "")
    .trim();
};

// Helper to normalize Arabic text (remove Tashkeel and unify letters)
export const normalizeArabic = (text: string): string => {
  if (!text) return "";
  
  // Robust Arabic normalization:
  // 1. NFD separates characters from marks
  // 2. \p{M} removes ALL diacritics/decoration marks
  // 3. NFC brings it back to standard form
  return text.normalize('NFD')
    .replace(/\p{M}/gu, '')
    .normalize('NFC')
    .replace(/[أإآٱ]/g, "ا")
    .replace(/ة/g, "ه")
    .replace(/[ىي]/g, "ي")
    .replace(/ؤ/g, "و")
    .replace(/ئ/g, "ي")
    .replace(/[\u060C\u061B\u061F\u06D4۝]/g, "")
    .replace(/[٠-٩0-9]/g, "")
    .trim()
    .replace(/\s+/g, " ");
};

export const searchInQuran = async (keyword: string) => {
  if (!keyword || keyword.trim().length < 2) return [];
  
  const cleanKeyword = normalizeArabic(keyword);
  
  try {
    // If index doesn't exist, fetch it once
    if (!fullQuranSearchIndex && !isFetchingIndex) {
      isFetchingIndex = true;
      try {
        const response = await fetchWithRetry('https://api.alquran.cloud/v1/quran/quran-simple-clean');
        const data = await safeJson(response);
        if (data.status === 'OK' && data.data && data.data.surahs) {
          fullQuranSearchIndex = data.data.surahs.flatMap((s: any) => {
            if (!s) return [];
            return (s.ayahs || []).map((a: any) => ({
              text: a.text,
              normalizedText: normalizeArabic(a.text),
              surahNumber: s.number,
              surahName: s.name || `Sura ${s.number}`,
              englishSurahName: QURAN_SURAHS[s.number - 1]?.englishName || s.englishName || `Surah ${s.number}`,
              ayahNumber: a.numberInSurah
            }));
          });
        }
      } catch (e) {
        console.error('Failed to load local index, falling back to per-request search', e);
      } finally {
        isFetchingIndex = false;
      }
    }

    // Perform local search on the normalized index
    // This solves the Hamza issue and matches the accuracy of professional apps
    if (fullQuranSearchIndex) {
      const matches = fullQuranSearchIndex.filter(ayah => 
        ayah.normalizedText.includes(cleanKeyword)
      );

      // Return limit for UI performance, but we have all matches
      return matches.slice(0, 100).map(match => ({
        text: match.text,
        surahNum: match.surahNumber,
        surahName: match.surahName,
        englishSurahName: match.englishSurahName,
        ayahNum: match.ayahNumber
      }));
    }

    // Fallback to API search if local indexing is still in progress or failed
    try {
      const response = await fetchWithRetry(`https://api.alquran.cloud/v1/search/${encodeURIComponent(cleanKeyword)}/all/quran-simple-clean`);
      const data = await safeJson(response);
      
      if (!data.data || !data.data.matches) return [];

      return data.data.matches.map((match: any) => {
        const s = match.surah || {};
        return {
          text: match.text,
          surahNum: s.number,
          surahName: s.name || `Sura ${s.number}`,
          englishSurahName: QURAN_SURAHS[s.number - 1]?.englishName || s.englishName || `Surah ${s.number}`,
          ayahNum: match.numberInSurah
        };
      });
    } catch (e) {
      // Final fallback search on api.quran.com if alquran.cloud is completely down
      const fallbackRes = await fetchWithRetry(`https://api.quran.com/api/v4/search?q=${encodeURIComponent(cleanKeyword)}&language=ar`);
      const fallbackData = await safeJson(fallbackRes);
      
      if (!fallbackData.search || !fallbackData.search.results) return [];
      
      return fallbackData.search.results.map((res: any) => {
        const [surahNum, ayahNum] = res.verse_key.split(':');
        const surah = QURAN_SURAHS[parseInt(surahNum) - 1];
        return {
          text: res.text,
          surahNum: parseInt(surahNum),
          surahName: surah?.name || `سورة ${surahNum}`,
          englishSurahName: surah?.englishName || `Surah ${surahNum}`,
          ayahNum: parseInt(ayahNum)
        };
      });
    }
  } catch (error) {
    console.error('Error searching ayah:', error);
    return [];
  }
};
